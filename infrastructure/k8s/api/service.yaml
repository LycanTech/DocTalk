apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: doctalk
  labels:
    app: api
    component: backend
    app.kubernetes.io/part-of: doctalk
spec:
  selector:
    app: api
  ports:
    - port: 4000
      targetPort: 4000
      name: http
