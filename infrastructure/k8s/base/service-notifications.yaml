apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-notifications
  namespace: doctalk
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-notifications
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: service-notifications
    spec:
      containers:
        - name: service-notifications
          image: doctalk-notifications:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 4015
          envFrom:
            - configMapRef:
                name: doctalk-config
            - secretRef:
                name: doctalk-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: 4015
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 4015
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "384Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: service-notifications
  namespace: doctalk
spec:
  selector:
    app: service-notifications
  ports:
    - port: 4015
      targetPort: 4015
  type: ClusterIP
