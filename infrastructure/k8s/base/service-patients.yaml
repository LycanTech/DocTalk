apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-patients
  namespace: doctalk
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-patients
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: service-patients
    spec:
      containers:
        - name: service-patients
          image: doctalk-patients:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 4012
          envFrom:
            - configMapRef:
                name: doctalk-config
            - secretRef:
                name: doctalk-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: 4012
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 4012
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "384Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: service-patients
  namespace: doctalk
spec:
  selector:
    app: service-patients
  ports:
    - port: 4012
      targetPort: 4012
  type: ClusterIP
