apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-auth
  namespace: doctalk
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-auth
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: service-auth
    spec:
      containers:
        - name: service-auth
          image: doctalk-auth:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 4011
          envFrom:
            - configMapRef:
                name: doctalk-config
            - secretRef:
                name: doctalk-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: 4011
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 4011
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "384Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: service-auth
  namespace: doctalk
spec:
  selector:
    app: service-auth
  ports:
    - port: 4011
      targetPort: 4011
  type: ClusterIP
